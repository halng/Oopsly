name: Deploy Service

on:
  workflow_call:
    inputs:
      git_hash:
        required: false
        description: The git hash to deploy. The workflow will be cancelled if the git hash is not provided.
        type: string
      git_tag:
        required: false
        description: The git tag to deploy. The workflow will be cancelled if the git tag is not provided.
        type: string
      env:
        required: true
        type: string
        description: The environment to deploy to.
    secrets:
      RAILWAY_TOKEN:
        required: true

jobs:
  validate-inputs:
    runs-on: ubuntu-latest
    outputs:
      release-ref: ${{ steps.check.outputs.tag }}
    steps:
      - name: Check deployment parameters
        id: check
        run: |
          if [[ -z "${{ inputs.git_hash }}" && -z "${{ inputs.git_tag }}" ]]; then
            echo "Error: Either git_hash or git_tag must be provided."
            exit 1
          fi

          if [[ -n "${{ inputs.git_hash }}" && -n "${{ inputs.git_tag }}" ]]; then
            echo "Error: Only one of git_hash or git_tag should be provided."
            exit 1
          fi

          if [[ -n "${{ inputs.git_hash }}" ]]; then
            echo "tag=${{ inputs.git_hash }}" >> $GITHUB_OUTPUT
          else
            echo "tag=${{ inputs.git_tag }}" >> $GITHUB_OUTPUT
          fi
      - name: Create Image Name
        run: |
          REPO_LOWER="${GITHUB_REPOSITORY,,}"
          echo "IMAGE_NAME=ghcr.io/${REPO_LOWER}/oopsly-api:${{ steps.check.outputs.tag }}" >> ${GITHUB_ENV}
  

  build:
    runs-on: ubuntu-latest
    needs: validate-inputs
    if: ${{ needs.validate-inputs.outputs.release-ref != '' }}
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
        with:
          fetch-depth: 0
          ref: ${{ needs.validate-inputs.outputs.release-ref }}

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Set up Java env
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: 21

      - name: Set up Gradle
        uses: gradle/actions/setup-gradle@v5
      - name: Build Native Docker Image
        run: ./gradlew bootBuildImage --imageName=${{ env.IMAGE_NAME }}
        env:
          GITHUB_REPOSITORY: ${{ github.repository }}
        working-directory: app/api

      - name: Log in to GitHub Container Registry
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Push Docker Image
        run: docker push ${{ env.IMAGE_NAME }}
        env:
          GITHUB_REPOSITORY: ${{ github.repository }}
        working-directory: app/api

  deploy:
    environment:
      name: ${{ inputs.env }}_approval
    runs-on: ubuntu-latest
    needs: [validate-inputs, build]
    steps:
      - name: Deploy to Railway
        run: |
          npm install -g @railway/cli
          railway up --environment ${{ inputs.env }} --image ${{ env.IMAGE_NAME }} --tag ${{ needs.validate-inputs.outputs.release-ref }}
        env:
          RAILWAY_TOKEN: ${{ secrets.RAILWAY_TOKEN }}
