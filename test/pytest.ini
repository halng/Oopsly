# pytest.ini
[pytest]
addopts =
    --verbose
    --screenshot=only-on-failure
    --video=retain-on-failure
    --tracing=retain-on-failure
    --output=test-results
    -n auto

testpaths = tests
python_files = test_*.py