# Database Configuration
DB_HOST=localhost
DB_PORT=5432
DB_NAME=oopsly
DB_USERNAME=postgres
DB_PASSWORD=postgres

# Redis Configuration
REDIS_HOST=localhost
REDIS_PORT=6379

# JWT Configuration
JWT_SECRET=your_secure_jwt_secret_here_min_256_bits

# CORS Configuration
# Comma-separated list of allowed origins (only for dev profile)
ALLOWED_ORIGINS=http://localhost:8081